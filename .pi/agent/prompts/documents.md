---
description: Update docs and lattices so they match the current code
---
Please update the project documentation and lattice docs so they reflect the code as it exists now.

Read the relevant code first and do not guess from old docs. Update only the docs that are actually stale, keep the edits minimal, accurate, and specific, and prefer one canonical current-state description by removing stale or misleading docs instead of preserving old behavior unless explicitly asked not to. If code and docs disagree, treat the code as the source of truth unless there is clear evidence the code is wrong. When touching `lat-md/` or `@lat:` anchors, use the `lat-md` skill, and verify any lattice updates with the shared lattice verification helper. End with the files changed, assumptions made, verification run, remaining doc debt if any, and ambiguities that require user judgment if any.
